<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- selected -->
    <item android:drawable="@drawable/bg_black_color_selector" android:state_selected="true"/>

    <!-- pressed -->
    <item android:drawable="@drawable/bg_black_color_selector" android:state_pressed="true"/>

    <!-- focused -->
    <item android:drawable="@drawable/bg_black_color_selector" android:state_focused="true"/>

    <!-- default -->
    <item android:drawable="@color/White"/>

</selector>